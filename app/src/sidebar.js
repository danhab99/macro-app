import React from "react";
import { Box } from 'theme-ui'

export default function SidebarContent(props) {
  return (
    <Box
      sx={{
        minWidth: "200px",
        height: "100vh",
        bg: "backgroundLight",
      }}
    >

    </Box>
  );
}
